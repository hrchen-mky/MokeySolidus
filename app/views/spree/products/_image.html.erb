<% if defined?(image) && image %>
  <%= render 'spree/shared/image', image: image, size: :product, itemprop: "image", id: "expandedImg" %>
<% else %>
  <%= render 'spree/shared/image', image: @product.gallery.images.first, size: :product, itemprop: "image", id: "expandedImg"%>
<% end %>
